const LoginPage = () => {
  const flexBetween = 'flex justify-center items-center'
  return (
    <div className={`${flexBetween} flex-col h-screen`}>
      <p className='prose-xl'>Log In</p>
    </div>
  )
}

export default LoginPage
